---
const { aspect = "3/2", class: className = "" } = Astro.props;
---

<div class={`placeholder ${className}`} style={`--aspect:${aspect}`}></div>

<style>
    .placeholder {
        width: 100%;
        aspect-ratio: var(--aspect);
        background: var(--black);
    }
</style>