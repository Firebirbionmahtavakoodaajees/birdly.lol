<nav class="nav">
  <div class="nav-bg" aria-hidden="true">
    <span></span><span></span><span></span>
    <span></span><span></span><span></span>
    <span></span><span></span><span></span>
    <span></span><span></span><span></span>
    <span></span><span></span><span></span>
    <span></span><span></span><span></span>
  </div>

  <div class="container bar">
    <a class="brand" href="/">
      <img src="/favicon.png" alt="Birdly logo" class="brand-logo" />
      <span class="brand-line"></span>
      Birdly
    </a>

    <div class="menu">
      <input type="checkbox" id="menu-toggle" class="menu-toggle" aria-hidden="true" />

      <label for="menu-toggle" class="menu-button" aria-label="Toggle menu">
        <span></span><span></span><span></span>
      </label>

      <div class="links">
        <a href="/#showcase">Showcase</a>
        <a href="/photos">All Pictures</a>
        <a href="/client-galleries">Client Galleries</a>
        <a href="/pricing">Pricing</a>
        <a href="/contact">Contact</a>
        <a href="/account" class="book-a-time">Account</a>
        <a href="/booking" class="book-a-time" style="color: black">Book a time!</a>
      </div>
    </div>
  </div>
</nav>

<style>
  .book-a-time {
    color: var(--lavender);
    font-weight: 900;
  }

  .brand-logo {
    height: 20px;
    display: block;
  }

  .brand-line {
    width: 14px;
    height: 2px;
    background: var(--black);
  }

  .nav {
    position: fixed;
    inset: 0 0 auto;
    background: linear-gradient(to right, var(--white) 60%, #f2eeff 100%);
    border-bottom: 2px solid var(--lavender);
    z-index: 20;
    width: 100%;
    overflow-x: clip;
  }

  .nav-bg {
    position: absolute;
    inset: 0;
    pointer-events: none;
    z-index: 0;
    overflow: hidden;
  }

  .nav-bg > span {
    position: absolute;
    width: 6px;
    height: 6px;
    background: var(--lavender);
    opacity: 0.75;
    display: block;
  }

  .nav-bg > span:nth-child(1)  { top: 20%; left: 5%;  transform: rotate(23deg); }
  .nav-bg > span:nth-child(2)  { top: 55%; left: 10%; border-radius: 50%; }
  .nav-bg > span:nth-child(3)  { top: 30%; left: 15%; transform: rotate(-41deg); clip-path: polygon(50% 0, 100% 100%, 0 100%); }
  .nav-bg > span:nth-child(4)  { top: 65%; left: 20%; transform: rotate(12deg); }
  .nav-bg > span:nth-child(5)  { top: 25%; left: 25%; border-radius: 50%; }
  .nav-bg > span:nth-child(6)  { top: 60%; left: 30%; transform: rotate(-18deg); clip-path: polygon(50% 0, 100% 100%, 0 100%); }
  .nav-bg > span:nth-child(7)  { top: 35%; left: 35%; transform: rotate(52deg); }
  .nav-bg > span:nth-child(8)  { top: 65%; left: 40%; border-radius: 50%; }
  .nav-bg > span:nth-child(9)  { top: 25%; left: 45%; transform: rotate(-27deg); clip-path: polygon(50% 0, 100% 100%, 0 100%); }
  .nav-bg > span:nth-child(10) { top: 55%; left: 50%; transform: rotate(8deg); }
  .nav-bg > span:nth-child(11) { top: 30%; left: 55%; border-radius: 50%; }
  .nav-bg > span:nth-child(12) { top: 70%; left: 60%; transform: rotate(-33deg); clip-path: polygon(50% 0, 100% 100%, 0 100%); }
  .nav-bg > span:nth-child(13) { top: 20%; left: 65%; transform: rotate(45deg); }
  .nav-bg > span:nth-child(14) { top: 60%; left: 70%; border-radius: 50%; }
  .nav-bg > span:nth-child(15) { top: 30%; left: 75%; transform: rotate(-15deg); clip-path: polygon(50% 0, 100% 100%, 0 100%); }
  .nav-bg > span:nth-child(16) { top: 65%; left: 82%; transform: rotate(28deg); }
  .nav-bg > span:nth-child(17) { top: 25%; left: 90%; border-radius: 50%; }
  .nav-bg > span:nth-child(18) { top: 60%; left: 95%; transform: rotate(-50deg); clip-path: polygon(50% 0, 100% 100%, 0 100%); }

  .bar {
    min-height: var(--nav-height);
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    position: relative;
    z-index: 1;
    box-sizing: border-box;
    width: 100%;
  }

  .brand,
  .links a {
    text-decoration: none;
  }

  .brand {
    font-size: 1.4rem;
    font-weight: 600;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    letter-spacing: -0.02em;
  }

  .menu {
    position: relative;
  }

  .menu-toggle {
    display: none;
  }

  .menu-button {
    cursor: pointer;
    display: none;
    flex-direction: column;
    gap: 5px;
    padding: 8px;
  }

  .menu-button span {
    width: 24px;
    height: 2px;
    background: var(--black);
    display: block;
  }

  .links {
    display: flex;
    gap: 1.25rem;
    flex-wrap: wrap;
    justify-content: flex-end;
    font-family: system-ui, sans-serif;
    font-size: 0.85rem;
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }

  .links a {
    position: relative;
    padding: 4px 0;
  }

  .links a::after {
    content: "";
    position: absolute;
    left: 0;
    bottom: 0;
    width: 100%;
    height: 2px;
    background: var(--lavender);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.2s;
  }

  .links a:hover::after {
    transform: scaleX(1);
  }

  @media (max-width: 640px) {
    .bar {
      padding: 0.75rem 1rem;
    }

    .menu-button {
      display: flex;
    }

    .menu-button span {
      width: 24px;
      height: 2px;
      background: var(--black);
      display: block;
      transition: transform 0.25s ease, opacity 0.25s ease;
    }

    .menu > .links {
      position: absolute;
      top: calc(100% + 12px);
      right: 0;
      background: var(--white);
      border: 2px solid var(--lavender);
      padding: 1rem 1.5rem;
      flex-direction: column;
      align-items: flex-start;
      gap: 1rem;
      min-width: 180px;
      max-width: calc(100vw - 2rem);
      box-sizing: border-box;
      z-index: 30;
      opacity: 0;
      transform: translateY(-8px);
      pointer-events: none;
      transition: opacity 0.2s ease, transform 0.2s ease;
    }

    .menu-toggle:checked ~ .links {
      display: flex;
      position: absolute;
      top: calc(100% + 12px);
      right: 0;
      background: var(--white);
      border: 2px solid var(--lavender);
      padding: 1rem 1.5rem;
      flex-direction: column;
      align-items: flex-start;
      gap: 1rem;
      min-width: 180px;
      max-width: calc(100vw - 2rem);
      box-sizing: border-box;
      z-index: 30;
      opacity: 1;
      transform: translateY(0);
      pointer-events: auto;
    }

    .menu-toggle:checked ~ .menu-button span:nth-child(1) {
      transform: translateY(7px) rotate(45deg);
    }

    .menu-toggle:checked ~ .menu-button span:nth-child(2) {
      opacity: 0;
    }

    .menu-toggle:checked ~ .menu-button span:nth-child(3) {
      transform: translateY(-7px) rotate(-45deg);
    }
  }
</style>